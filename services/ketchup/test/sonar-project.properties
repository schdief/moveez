# must be unique in a given SonarQube instance
sonar.projectKey=schdief:ketchup
# this is the name and version displayed in the SonarQube UI. Was mandatory prior to SonarQube 6.1.
sonar.projectName=ketchup
sonar.projectVersion=1.0.0
 
# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
# This property is optional if sonar.modules is set. 
sonar.sources=../ketchup.js
 
# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8

# setting sonarcloud organization
sonar.organization=schdief-github

# setting folders to scan
sonar.text=test

# setting path to code coverage report from istanbul in lcov format
sonar.javascript.lcov.reportPaths=./coverage/lcov.info

# exclude tests from coverage measurements. We don't test tests ;-)
sonar.coverage.exclusions=../test/*
